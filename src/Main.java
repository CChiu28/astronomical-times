import java.util.HashMap;
import java.util.Map;

public class Main {

	public static void main(String[] args) {
		gui.execute(args);
		//String url = "brooklyn college";
		//getData data = new getData();
		
		/*
		
		try {
			Map<String,String> result = new HashMap<String,String>();
			// Using geoCode to generate a map that will be used in sendGet to retrieve the proper formatted information
			result = data.geoCode(url);
			String formatted_info = data.sendGET(result.get("lon"), result.get("lat") /*date to be added);
			System.out.println(formatted_info);
		} catch (Exception e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		} 
		
		*/
	}
}

