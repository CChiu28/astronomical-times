import com.jfoenix.controls.JFXButton;
import javafx.geometry.Insets;
import javafx.geometry.Pos;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.VBox;
import javafx.scene.paint.Color;
import javafx.scene.text.Font;
import javafx.stage.Stage;

/*
 * This class sets up the splash screen for the app. The button here initializes the core class
 * for the app.
 */
public class TitleScene {
	private VBox titleScene;
//	public VBox Title(Stage stage) {
//		titleScene = new VBox(20);
//		
//		Label title = new Label("Astronomical Times");
//		title.setPadding(new Insets(10, 10, 100, 10));
//		title.setFont(Font.font(48));
//		title.setTextFill(Color.WHITE);
//		
//		Label summary = new Label("The Astronomical Time calculator can be used to find the sunrise, sunset, \n      astronomical twilight and much more information at any location.");
//		summary.setPadding(new Insets(10, 10, 10, 10));
//		summary.setTextFill(Color.WHITE);
//		
//		JFXButton enter = new JFXButton("Enter");
//		enter.setPadding(new Insets(10, 10, 10, 10));
//		enter.setOnAction(e -> stage.setScene(new MainScene().mainScene()));
//		
//		titleScene.getChildren().addAll(title, summary, enter);
//		titleScene.setAlignment(Pos.CENTER);
//		
//		return titleScene;
//	}
}
